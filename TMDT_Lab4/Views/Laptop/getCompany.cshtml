@model IEnumerable<string>
    <div id="2">
        @foreach (var item in Model)
        {
            @Html.CheckBox(item)
            @Html.Label(item)
            <b>&nbsp;</b>
        }
    </div>

    @*<select >
        @{ 
            foreach(var item in Model)
            {
                <option  value="item" customAttribute="2" id="2">
                    @Html.Label(item)
                </option>
            }
        }
    </select>*@
